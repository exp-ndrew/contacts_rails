<h1>Contacts</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li>
      <a href="/contacts/<%= contact.id %>"> <%= contact.name %> </a>
    </li>
  <% end %>
</ul>

<a href="contacts/new"><p> Create New Contact </p></a>
